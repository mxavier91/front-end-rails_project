{{#each movies as |movie|}}

<ul>
  <h1 data-id="{{movie.id}}">
     <small class="main-title">
       Title:{{movie.title}}
     </small>
   </h1>
    {{#if movie.title}}
<<<<<<< HEAD
      <li class="listings">Moive ID:{{movie.id}}</li>
      <li class="listings">Director:{{movie.director}}</li>
      <li class="listings">Actor:{{movie.actor}}</li>
=======
      <li class="id">Moive ID:{{movie.id}}</li>
      <li class="director">Director:{{movie.director}}</li>
      <li class="actor">Actor:{{movie.actor}}</li>
>>>>>>> feature
    {{else}}
      Nothing provided
    {{/if}}<br>
    <button class="movie-delete" data-id={{movie.id}}>
      Remove
    </button>
</ul>

  {{/each}}

</ul>
