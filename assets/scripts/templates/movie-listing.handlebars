{{#each movies as |movie|}}

  <!-- {{> partial}}
  Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <h1 data-id="{{movie.id}}"> <small>Title:{{movie.title}}</small></h1>
  <div>
    {{#if movie.director}}
      {{movie.title}}
      {{movie.director}}
      {{movie.actor}}
    {{else}}
      No author provided.
    {{/if}}
  </div>

{{/each}}
